# AI-Swing-Breakout-Pro Project Context

Treat my GitHub repository as the single source of truth.

Repository:
ZiXXXiZ/AI-Swing-Breakout-Pro (Private)

Project Root:
C:\Users\kkk\AppData\Roaming\MetaQuotes\Terminal\829BEA48CFE0CB726192D822F91AD6B5\MQL5\Experts\AI_SwingBreakout_Pro

Rules:

* All source files are inside the project root.
* Never use `<Core/...>` or global Include paths.
* Always use project-relative `#include` paths.
* Generate production-quality MQL5 code only.
* Deliver complete source files instead of partial snippets.
* Avoid repeating explanations; keep responses concise.
* Maintain architecture consistency.
* Assume the repository is the source of truth if GitHub access is available.

Current Project Status:

Completed:

* Constants.mqh
* Types.mqh
* TradeStructures.mqh
* MarketStructures.mqh
* RiskStructures.mqh
* AccountStructures.mqh
* StatisticsStructures.mqh

Current Task:

Rebuild Include/Core/MathUtils.mqh from scratch as a production-quality file. Do not reuse the previous version.

After MathUtils.mqh is complete, continue with:

1. Platform.mqh
2. Logger.mqh
3. ValidationUtils.mqh
4. RiskManager
5. TradeManager

Use "LDN" (Let Do Next) to continue to the next planned task without re-explaining previous work.